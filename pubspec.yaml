name: model_generator
description: A sample command-line application.
version: 1.0.0
repository: https://github.com/Yoeri-z/model_generator
executables:
  mgen: model_generator
environment:
  sdk: ^3.0.3

# Add regular dependencies here.
dependencies:
  yaml: ^3.1.2
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
